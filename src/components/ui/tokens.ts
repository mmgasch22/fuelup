// Colores semánticos para kcal/macros. Referencian las variables CSS de
// globals.css — cambiar la paleta es editar ese archivo, no esto.
export const macroColors = {
  kcal: "var(--primary)",
  protein: "var(--protein)",
  carbs: "var(--carbs)",
  fat: "var(--fat)",
} as const;
